# Catalog entry for VC MIPI sensor IMX183.
#
# Populated from the driver's canonical source
# (vc_mipi_modules.c, raspberrypi/develop branch).
# No live V4L2/mediabus values here — see the parent
# sensor_profiles/ directory for live-verified profiles.

sensor_name: IMX183
display_name: "IMX183 (VC MIPI, rolling shutter, color+mono)"
vendor: sony
module_id: "0x0183"

native:
  width: 5440
  height: 3648

shutter: rolling
color_mono: "color+mono"

modes:
  mode_count: 6
  lane_options: [2, 4]
  format_options: ['RAW08', 'RAW10', 'RAW12']
  has_binning: false

verification:
  status: catalog_only
  source: "https://github.com/VC-MIPI-modules/vc_mipi_core/blob/raspberrypi/develop/src/vc_mipi_modules.c#L223"
  verified_at: "2026-08-04"
