# Catalog entry for VC MIPI sensor IMX392.
#
# Populated from the driver's canonical source
# (vc_mipi_modules.c, raspberrypi/develop branch).
# No live V4L2/mediabus values here — see the parent
# sensor_profiles/ directory for live-verified profiles.

sensor_name: IMX392
display_name: "IMX392 (VC MIPI, global shutter, color+mono)"
vendor: sony
module_id: "0x0392"

native:
  width: 1920
  height: 1200

shutter: global
color_mono: "color+mono"

modes:
  mode_count: 6
  lane_options: [2, 4]
  format_options: ['RAW08', 'RAW10', 'RAW12']
  has_binning: false

verification:
  status: catalog_only
  source: "https://github.com/VC-MIPI-modules/vc_mipi_core/blob/raspberrypi/develop/src/vc_mipi_modules.c#L536"
  verified_at: "2026-08-04"
